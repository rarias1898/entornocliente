export interface Planet {
  name: string,
  rotation_period: number,
  orbital_period: number,
  diameter: number,
  gravity: string,
  terrain: string,
  surface_water: number,
  population: number
}